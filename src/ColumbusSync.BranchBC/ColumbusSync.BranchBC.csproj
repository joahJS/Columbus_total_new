<?xml version="1.0" encoding="utf-8"?>
<Project ToolsVersion="15.0" DefaultTargets="Build" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <Import Project="$(MSBuildExtensionsPath)\$(MSBuildToolsVersion)\Microsoft.Common.props" Condition="Exists('$(MSBuildExtensionsPath)\$(MSBuildToolsVersion)\Microsoft.Common.props')" />
  <PropertyGroup>
    <Configuration Condition=" '$(Configuration)' == '' ">Debug</Configuration>
    <Platform Condition=" '$(Platform)' == '' ">x86</Platform>
    <ProjectGuid>{80A53FF6-4F8C-4BDC-8C08-E1D085264439}</ProjectGuid>
    <OutputType>Exe</OutputType>
    <RootNamespace>ColumbusSync.BranchBC</RootNamespace>
    <AssemblyName>ColumbusSync.BranchBC</AssemblyName>
    <TargetFrameworkVersion>v4.6.1</TargetFrameworkVersion>
    <FileAlignment>512</FileAlignment>
    <AutoGenerateBindingRedirects>true</AutoGenerateBindingRedirects>
    <Deterministic>true</Deterministic>
    <!--
      독립 배포 프로젝트: DevExpress나 다른 ColumbusSync/ColumbusWeighing 프로젝트를
      참조하지 않는다.

      mdb(Access)를 읽는 ACE OLEDB 12.0 드라이버는 PC마다 32비트로 깔려있을 수도, 64비트로
      깔려있을 수도 있어서, 이 프로젝트는 x86/x64 두 가지 플랫폼으로 모두 빌드해둔다.
      실제 배포 시에는 ColumbusSync.BranchBC.Launcher가 그 PC에 설치된 드라이버 비트수를
      보고 맞는 쪽을 자동으로 실행해준다(그 프로젝트의 README 참고). OutputPath에 플랫폼
      이름을 포함시켜, x86/x64 빌드 결과물이 서로 겹쳐쓰지 않고 별도 폴더에 쌓이게 했다.
    -->
  </PropertyGroup>
  <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Debug|x86' ">
    <DebugSymbols>true</DebugSymbols>
    <DebugType>full</DebugType>
    <Optimize>false</Optimize>
    <OutputPath>bin\x86\Debug\</OutputPath>
    <DefineConstants>DEBUG;TRACE</DefineConstants>
    <ErrorReport>prompt</ErrorReport>
    <WarningLevel>4</WarningLevel>
    <LangVersion>7.0</LangVersion>
    <PlatformTarget>x86</PlatformTarget>
  </PropertyGroup>
  <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Release|x86' ">
    <DebugType>pdbonly</DebugType>
    <Optimize>true</Optimize>
    <OutputPath>bin\x86\Release\</OutputPath>
    <DefineConstants>TRACE</DefineConstants>
    <ErrorReport>prompt</ErrorReport>
    <WarningLevel>4</WarningLevel>
    <LangVersion>7.0</LangVersion>
    <PlatformTarget>x86</PlatformTarget>
  </PropertyGroup>
  <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Debug|x64' ">
    <DebugSymbols>true</DebugSymbols>
    <DebugType>full</DebugType>
    <Optimize>false</Optimize>
    <OutputPath>bin\x64\Debug\</OutputPath>
    <DefineConstants>DEBUG;TRACE</DefineConstants>
    <ErrorReport>prompt</ErrorReport>
    <WarningLevel>4</WarningLevel>
    <LangVersion>7.0</LangVersion>
    <PlatformTarget>x64</PlatformTarget>
  </PropertyGroup>
  <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Release|x64' ">
    <DebugType>pdbonly</DebugType>
    <Optimize>true</Optimize>
    <OutputPath>bin\x64\Release\</OutputPath>
    <DefineConstants>TRACE</DefineConstants>
    <ErrorReport>prompt</ErrorReport>
    <WarningLevel>4</WarningLevel>
    <LangVersion>7.0</LangVersion>
    <PlatformTarget>x64</PlatformTarget>
  </PropertyGroup>
  <ItemGroup>
    <Reference Include="System" />
    <Reference Include="System.Configuration" />
    <Reference Include="System.Core" />
    <Reference Include="System.Data" />
    <Reference Include="System.Xml" />
  </ItemGroup>
  <ItemGroup>
    <Compile Include="Config\SyncSettings.cs" />
    <Compile Include="Hub\HubWriter.cs" />
    <Compile Include="Hub\SqlHelper.cs" />
    <Compile Include="Logging\FileLogger.cs" />
    <Compile Include="Program.cs" />
    <Compile Include="Properties\AssemblyInfo.cs" />
    <Compile Include="Source\Dto.cs" />
    <Compile Include="Source\MdbSourceReader.cs" />
    <Compile Include="Source\OleDbHelper.cs" />
    <Compile Include="SyncOrchestrator.cs" />
  </ItemGroup>
  <ItemGroup>
    <!--
      App.config는 Git 추적 대상이 아니다(README 참고) — 빌드하려면 App.config.example을
      복사해서 App.config로 만들어야 한다. MSBuild는 실제 파일이 디스크에 있으면 되므로
      git 추적 여부와 무관하게 정상적으로 참조/복사한다.
    -->
    <None Include="App.config" />
  </ItemGroup>
  <Import Project="$(MSBuildToolsPath)\Microsoft.CSharp.targets" />
</Project>
